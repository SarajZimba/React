const initialData = {
    game : " Football",
    player : "Messi"

}

export const gameReducer = (state=initialData, action) => {
    switch(action.type){
        default :
        return state;
    }
}